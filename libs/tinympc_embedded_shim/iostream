#pragma once

#include <string>

namespace std {

class TinyMpcNullStream {
 public:
  template <typename T>
  TinyMpcNullStream &operator<<(const T &) {
    return *this;
  }
};

inline TinyMpcNullStream cout;
inline TinyMpcNullStream cerr;

inline TinyMpcNullStream &endl(TinyMpcNullStream &stream) { return stream; }

}  // namespace std
